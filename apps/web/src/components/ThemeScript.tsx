// ThemeScript and BrandingScript were removed — inline scripts now live
// directly in apps/core/src/app/[locale]/layout.tsx to avoid React 19's
// "scripts inside components are never executed on the client" warning.
